import{_ as o,c as a,o as s,ae as t}from"./chunks/framework.iOmxR4Tm.js";const p=JSON.parse('{"title":"结构化 数据清洗与 QA 构建流程解析","description":"","frontmatter":{},"headers":[],"relativePath":"guide/structured-data-cleaning.md","filePath":"guide/structured-data-cleaning.md"}'),d={name:"guide/structured-data-cleaning.md"};function i(c,e,l,n,r,g){return s(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="结构化-数据清洗与-qa-构建流程解析" tabindex="-1">结构化 数据清洗与 QA 构建流程解析 <a class="header-anchor" href="#结构化-数据清洗与-qa-构建流程解析" aria-label="Permalink to &quot;结构化 数据清洗与 QA 构建流程解析&quot;">​</a></h1><p>本文聚焦 结构化 <code>结构化-0.3.02</code> 中的 CSV 数据清洗、问答构建与评分策略，帮助在 Qing-Agent 中复用相同的清洗逻辑。内容依托 <code>结构化/data/qa_generator.py</code>、<code>结构化/data/clean/strategies.py</code>、<code>结构化/data/chat_parsers/telegram_parser.py</code>、<code>结构化/prompts/clean_data.py</code> 等实现整理而成。</p><h2 id="_1-端到端流程概览" tabindex="-1">1. 端到端流程概览 <a class="header-anchor" href="#_1-端到端流程概览" aria-label="Permalink to &quot;1. 端到端流程概览&quot;">​</a></h2><ol><li><strong>源数据规范化</strong>：Telegram 等平台的原始导出（示例：<code>dataset/telegram/&lt;chat&gt;/result.json</code>）被 <code>TelegramChatParser</code> 转成统一 CSV（<code>dataset/csv/&lt;chat&gt;/&lt;chat&gt;.csv</code>），列包含 <code>id / MsgSvrID / type_name / is_sender / talker / room_name / msg / src / CreateTime / is_forward</code>。</li><li><strong>预处理与过滤</strong>（<code>load_csv</code>）：剔除跳过类型（系统通知、转账等）、自发的合并转发、含敏感词/PII 的文本，将找得到的他人图片标记为 <code>&lt;image&gt;</code> 并只保留相对路径。</li><li><strong>消息聚合</strong>（<code>group_consecutive_messages</code>）：按 <code>single_combine_time_window</code>（默认 2 分钟）把同一人连续消息拼接成一条，遇到 <code>cut_type</code>（图片、语音、分享等）或自己发送的图片时插入 <code>CutMessage</code> 作为边界。</li><li><strong>QA 匹配</strong>（<code>match_qa</code>）：状态机在 <code>qa_match_time_window</code>（默认 5 分钟）内匹配“对方提问/上下文”与“自己回复”，可累积多轮，超过 <code>messages_max_length</code> 或图片数大于 <code>max_image_num</code> 的样本会被丢弃。</li><li><strong>可选图像转写</strong>：开启 <code>vision_api.enable</code> 时，通过 <code>ImageToTextProcessor</code> 并行调用多模态 LLM，把 <code>&lt;image&gt;</code> 占位替换成 <code>[图片描述: ...]</code> 文本后清空 <code>images</code> 列表。</li><li><strong>LLM 评分</strong>：若 <code>clean_dataset.enable_clean</code> 为真，Offline（vLLM）或 Online 策略会把 QA 组装成特定 prompt，请求 LLM 给出 1–5 分，结果写回 <code>qa.score</code>。</li><li><strong>数据落盘</strong>：<code>save_result</code> 输出 <code>dataset/res_csv/sft/sft-my.json</code>，每条样本包含 <code>{id, time, score, system, messages, images}</code>。</li><li><strong>训练阶段过滤</strong>：<code>train_sft.py</code> 在正式喂给 LLaMA Factory 前调用 <code>LLMCleaningStrategy.clean()</code>，把 <code>score</code> 低于阈值 (<code>clean_dataset.llm.accept_score</code>, 默认 2) 的样本过滤到 <code>&lt;dataset&gt;-cleaned</code>。</li></ol><h2 id="_2-csv-来源与组织方式" tabindex="-1">2. CSV 来源与组织方式 <a class="header-anchor" href="#_2-csv-来源与组织方式" aria-label="Permalink to &quot;2. CSV 来源与组织方式&quot;">​</a></h2><h3 id="_2-1-telegram-解析" tabindex="-1">2.1 Telegram 解析 <a class="header-anchor" href="#_2-1-telegram-解析" aria-label="Permalink to &quot;2.1 Telegram 解析&quot;">​</a></h3><ul><li><code>TelegramChatParser</code> 负责将 Telegram <code>result.json</code> 里的消息转换为 <code>ChatMessage</code>，并映射 <code>media_type</code> → <code>type_name</code>（text/image/video/voice/sticker 等）。</li><li>解析要点： <ul><li><code>msg</code> 字段对复杂 text（字符串数组、带实体）做展开。</li><li><code>src</code> 保存多媒体文件路径；只有在 <code>include_type</code> 包含 <code>STICKER</code> 且内容为空时才把贴纸 emoji 写入 <code>msg</code>。</li><li><code>is_sender</code> 通过配置里的 <code>telegram_args.my_id</code> 判断。</li><li>转写完成的 <code>ChatMessage</code> 会被写入 CSV，同时把所有 <strong>他人发送的图片</strong> 复制到 <code>dataset/media/images</code> 以便后续引用。</li></ul></li></ul><h3 id="_2-2-csv-目录" tabindex="-1">2.2 CSV 目录 <a class="header-anchor" href="#_2-2-csv-目录" aria-label="Permalink to &quot;2.2 CSV 目录&quot;">​</a></h3><ul><li>解析后的 CSV 统一放在 <code>dataset/csv/&lt;会话文件夹&gt;/&lt;会话&gt;.csv</code>，文件名中 <code>_start_end.csv</code> 的 <code>start</code> 用于 <code>get_csv_files()</code> 排序，保证跨文件的时间顺序。</li><li>这些 CSV 即 <code>load_csv</code> 的输入，之后所有逻辑都以 <code>ChatMessage</code> 列表为基础。</li></ul><h2 id="_3-预处理与初筛-load-csv" tabindex="-1">3. 预处理与初筛 (<code>load_csv</code>) <a class="header-anchor" href="#_3-预处理与初筛-load-csv" aria-label="Permalink to &quot;3. 预处理与初筛 (\`load_csv\`)&quot;">​</a></h2><ol><li><strong>类型过滤</strong>：<code>skip_type_list</code>（多语言定义，包含“系统通知/转账/语音通话/引用回复”等）直接删除，<code>cut_type_list</code>（图片/语音/分享类）不会删除，但会在后续用于切分。</li><li><strong>自发合并转发</strong>：当 <code>is_sender==1 且 is_forward==True</code> 时移除，避免把复制/转入的内容算入个性语料。</li><li><strong>PII 与停用词</strong>： <ul><li>根据配置语言选 <code>ChinesePIIDetector</code> 或 <code>PIIDetector</code>；批量检测文本消息，命中则整行删除。</li><li><code>blocked_words</code> = 配置文件里的数组 ∪ <code>dataset/blocked_words.json</code> 中定义的词表；命中任意词也删除。</li></ul></li><li><strong>非文本消息</strong>： <ul><li><code>.gif</code> 统一归类为 sticker/动画表情，并清空 <code>src</code>。</li><li>图片：若在 <code>dataset/media/images</code> 找到同名文件且 <code>is_sender==0</code>，则把 <code>msg</code> 设为 <code>&lt;image&gt;</code>、<code>src</code> 写成相对路径 <code>images/&lt;file&gt;</code>、<code>modality</code> 置为 <code>IMAGE</code>；否则把 <code>type_name</code> 改成 <code>Cut</code>，后续直接当边界。</li><li>其他类型一律把 <code>msg</code> 清空，保证清洗阶段只围绕文本内容。</li></ul></li><li><strong>时间戳标准化</strong>：<code>CreateTime</code> 转成 <code>pandas.Timestamp</code>，为之后的窗口判断做准备。</li></ol><h2 id="_4-消息聚合-group-consecutive-messages" tabindex="-1">4. 消息聚合 (<code>group_consecutive_messages</code>) <a class="header-anchor" href="#_4-消息聚合-group-consecutive-messages" aria-label="Permalink to &quot;4. 消息聚合 (\`group_consecutive_messages\`)&quot;">​</a></h2><p>目的：把零碎的连续消息合并，减少 QA 匹配噪声，同时在特定情形插入 <code>CutMessage</code>。</p><ul><li><strong>合并策略</strong>：同一 <code>talker</code>、同一 <code>is_sender</code> 且消息间隔不超过 <code>single_combine_time_window</code>（秒）时加入同一组，最后用 <code>_combine_text</code> 拼接，必要时在句尾加换行。</li><li><strong>媒体拼接限制</strong>：合并结果若超过 <code>combine_msg_max_length</code> 会被截断；结合 <code>&lt;image&gt;</code> 占位符的个数裁剪 <code>src</code> 列表，防止多余图片。</li><li><strong>Cut 插入</strong>： <ul><li>当前消息属于 <code>cut_type</code> 或“自己发的图片”则立即输出 <code>CutMessage</code>（包含 <code>cut_type</code>、时间、发送方），用于 <code>match_qa</code> 在读取到它时强制 flush。</li><li>连续多个 cut 只保留一个，避免重复切段。</li></ul></li></ul><h2 id="_5-qa-组装-match-qa" tabindex="-1">5. QA 组装 (<code>match_qa</code>) <a class="header-anchor" href="#_5-qa-组装-match-qa" aria-label="Permalink to &quot;5. QA 组装 (\`match_qa\`)&quot;">​</a></h2><p><code>match_qa</code> 通过一个状态机把聚合后的消息转为 <code>QaPair</code> 列表，核心规则：</p><ol><li><strong>状态设计</strong>： <ul><li><code>WAITING_INSTRUCTION</code>：等待“对方”发送的消息，作为潜在的用户提问。</li><li><code>WAITING_RESPONSE</code>：等待“自己”发送的消息，作为回答。</li></ul></li><li><strong>时间窗口</strong>：使用 <code>qa_match_strategy</code>（默认 <code>TimeWindowStrategy</code>）对 <code>last_message</code> 与当前消息做时间差判断，超过 <code>qa_match_time_window</code> 会强制结束上一轮对话并存档。</li><li><strong>CutMessage 处理</strong>：一旦遇到 <code>CutMessage</code>，立即把当前对话落盘并重置状态，确保跨媒体或不可解析片段不会连在一起。</li><li><strong>多轮 QA</strong>：在一次 <code>WAITING_RESPONSE</code> 状态下，如果多次出现“对方消息 → 我方消息”且都满足时间窗，就会把多轮 <code>Q/A</code> 依次写入 <code>conversation_messages</code>，形成一个长上下文样本。</li><li><strong>图片聚合</strong>：只有<strong>来自对方</strong>的消息才会把 <code>src</code> 里的相对路径追加到 <code>conversation_images</code>，并在写入前检查不超过 <code>max_image_num</code>。</li><li><strong>长度/图片数量约束</strong>：<code>_save_current_qa_pair</code> 会计算当前会话所有 <code>Message.content</code> 的字符总数，超过 <code>messages_max_length</code>（默认 2048）或图片超限直接放弃。</li><li><strong>System Prompt</strong>：每个样本额外带一个 <code>system</code> 字段，内容来自配置 <code>default_system</code>；若 <code>add_time=True</code>，则会把当前对话的时间戳格式化追加在系统提示里。</li></ol><h2 id="_6-输出-qa-数据结构" tabindex="-1">6. 输出 QA 数据结构 <a class="header-anchor" href="#_6-输出-qa-数据结构" aria-label="Permalink to &quot;6. 输出 QA 数据结构&quot;">​</a></h2><p><code>save_result</code> 将内存中的 <code>QaPair</code> 列表写成 JSON。单条样本示例：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;time&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2024-08-01T12:34:56.000&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;score&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;system&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;你是一个乐于助人的私人助理。 Current datetime: 08-01 12:34:56&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;messages&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;role&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;user&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;content&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;下午去你那边拿机器，顺便帮我看下版本？&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">},</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;role&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;assistant&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;content&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;可以，晚上六点之前来就行，我提前打包好。&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;images&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;images/abcd1234.jpg&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ul><li><code>messages</code> 只包含 <code>user</code> 与 <code>assistant</code> 角色，顺序即对话顺序；<code>system</code> 不放在 messages 里。</li><li><code>images</code> 是对方发送的媒体相对路径；若启用图像转写并替换了 <code>&lt;image&gt;</code>，这里会被清空。</li><li><code>score</code> 初始为 0，LLM 评分后会更新到 1–5（含图样本在 Offline 策略中直接赋值为 6，确保清洗阶段不会被阈值过滤）。</li></ul><h2 id="_7-llm-清洗与评分" tabindex="-1">7. LLM 清洗与评分 <a class="header-anchor" href="#_7-llm-清洗与评分" aria-label="Permalink to &quot;7. LLM 清洗与评分&quot;">​</a></h2><h3 id="_7-1-策略入口" tabindex="-1">7.1 策略入口 <a class="header-anchor" href="#_7-1-策略入口" aria-label="Permalink to &quot;7.1 策略入口&quot;">​</a></h3><ul><li><code>clean_dataset.enable_clean=True</code> 时才会触发评分。</li><li>若 <code>online_llm_clear=True</code>，使用 <code>OlineLLMCleaningStrategy</code>，通过自定义 API (<code>base_url</code>, <code>llm_api_key</code>, <code>model_name</code>) 走在线模型；否则要求本地安装 vLLM (<code>llamafactory.extras.packages.is_vllm_available</code>)，使用 <code>LLMCleaningStrategy</code>。</li></ul><h3 id="_7-2-提示词与输入格式" tabindex="-1">7.2 提示词与输入格式 <a class="header-anchor" href="#_7-2-提示词与输入格式" aria-label="Permalink to &quot;7.2 提示词与输入格式&quot;">​</a></h3><ol><li><strong>Offline（vLLM）</strong>： <ul><li>在 <code>judge</code> 方法中，把每条样本转成 <code>messages_str</code>，形如：<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Q: ...</span></span>
<span class="line"><span>A: ...</span></span>
<span class="line"><span>Q: ...</span></span>
<span class="line"><span>A: ...</span></span></code></pre></div></li><li>套用 <code>CLEAN_PROMPT</code>（<code>结构化/prompts/clean_data.py</code>），并把 <code>{&quot;id&quot;: &quot;&lt;qa_id&gt;&quot;,&quot;messages&quot;: &quot;&lt;messages_str&gt;&quot;}</code> 作为 JSON 片段嵌入 prompt。</li><li>LLM 必须返回 <code>{&quot;id&quot;: &quot;&lt;qa_id&gt;&quot;,&quot;score&quot;: &lt;1-5&gt;}</code> 的 JSON，vLLM 通过 <code>QaPairScore</code> 模型做解析与校验。</li></ul></li><li><strong>Online</strong>： <ul><li>每个批次组装 <code>qa_list = [{&quot;id&quot;: qa.id, &quot;Q&quot;: &quot;...&quot;, &quot;A&quot;: &quot;...&quot;}]</code>，仅取首个 <code>user</code>/<code>assistant</code> 消息填充，随后传给 <code>ONLINE_LLM_CLEAN_PROMPT</code>。</li><li>在线模型需返回一串 JSON array，<code>OlineLLMCleaningStrategy</code> 会做正则裁剪（移除 \`\`\`json 包裹）并用 <code>QaPairScoreWithId</code> 解析。</li></ul></li></ol><h3 id="_7-3-结果写回与统计" tabindex="-1">7.3 结果写回与统计 <a class="header-anchor" href="#_7-3-结果写回与统计" aria-label="Permalink to &quot;7.3 结果写回与统计&quot;">​</a></h3><ul><li>评分成功即写入 <code>qa.score</code>; 解析失败或模型异常会把该条记为 0 分，并在日志中记录。</li><li>Offline 策略会用 pandas 打印 1–5 分布统计；在线策略同样输出分布表，便于观测清洗力度。</li></ul><h3 id="_7-4-分数的二次利用" tabindex="-1">7.4 分数的二次利用 <a class="header-anchor" href="#_7-4-分数的二次利用" aria-label="Permalink to &quot;7.4 分数的二次利用&quot;">​</a></h3><ul><li><code>make-dataset</code> 阶段只负责打分与写入 JSON。</li><li>真正的样本过滤发生在 <code>train_sft.py</code>：它读取 <code>dataset_dir/dataset_info.json</code>，找到原始文件与 <code>&lt;dataset&gt;-cleaned</code> 文件名，调用 <code>LLMCleaningStrategy.clean()</code>： <ul><li>重新打开原始 JSON，丢弃 <code>score &lt; accept_score</code> 的条目。</li><li>若全部被丢弃则回退到原始数据，避免训练数据集为空。</li></ul></li></ul><h2 id="_8-图像描述与媒体处理" tabindex="-1">8. 图像描述与媒体处理 <a class="header-anchor" href="#_8-图像描述与媒体处理" aria-label="Permalink to &quot;8. 图像描述与媒体处理&quot;">​</a></h2><ul><li><code>ImageToTextProcessor</code> 只有在 <code>vision_api.enable=True</code> 且提供 <code>api_url/api_key/model_name</code> 时启用。</li><li>工作流程： <ol><li>扫描 <code>QaPair</code> 的 <code>images</code>，拼出绝对路径（基于 <code>config.media_dir</code>）。</li><li>通过线程池批量把图片做 base64，并调用兼容 OpenAI 的 <code>/chat/completions</code>（可配置重试、并发数）。</li><li>获得描述后，按 <code>&lt;image&gt;</code> 占位的出现顺序逐个替换为 <code>[图片描述: ...]</code>，替换完成后清空 <code>images</code> 列表。</li></ol></li><li>这样可以把多模态样本转成纯文本，继续沿用同一清洗策略。</li></ul><h2 id="_9-关键配置开关" tabindex="-1">9. 关键配置开关 <a class="header-anchor" href="#_9-关键配置开关" aria-label="Permalink to &quot;9. 关键配置开关&quot;">​</a></h2><table tabindex="0"><thead><tr><th>配置项 (<code>结构化/utils/config_models.py</code>)</th><th>作用</th></tr></thead><tbody><tr><td><code>include_type</code> / <code>skip_type_list</code> / <code>cut_type_list</code></td><td>控制保留哪些模态、哪些消息需要切断或跳过。</td></tr><tr><td><code>single_combine_time_window</code> / <code>qa_match_time_window</code></td><td>决定消息合并与 QA 匹配的时间阈值。</td></tr><tr><td><code>combine_msg_max_length</code> / <code>messages_max_length</code></td><td>限制合并后单条消息和整段 QA 的最大长度。</td></tr><tr><td><code>max_image_num</code></td><td>每个 QA 可携带的图片数量。</td></tr><tr><td><code>blocked_words</code> + <code>dataset/blocked_words.json</code></td><td>自定义敏感词过滤表。</td></tr><tr><td><code>clean_dataset.llm.accept_score</code></td><td>二次过滤的及格线。</td></tr><tr><td><code>online_llm_clear</code>, <code>model_name</code>, <code>base_url</code>, <code>llm_api_key</code>, <code>clean_batch_size</code></td><td>在线 LLM 清洗所需的连接信息与批大小。</td></tr><tr><td><code>vision_api.*</code></td><td>图像转写 API 的开关与并发控制。</td></tr></tbody></table><h2 id="_10-在-qing-agent-中复用的建议" tabindex="-1">10. 在 Qing-Agent 中复用的建议 <a class="header-anchor" href="#_10-在-qing-agent-中复用的建议" aria-label="Permalink to &quot;10. 在 Qing-Agent 中复用的建议&quot;">​</a></h2><ol><li><strong>保持两阶段清洗</strong>：沿用“生成数据时打分 + 训练前按阈值过滤”的分层设计，这样旧流程可继续输出未清洗数据，而新的 default parser 能无缝插入。</li><li><strong>复制状态机与窗口逻辑</strong>：<code>group_consecutive_messages</code> 与 <code>match_qa</code> 的时间窗、Cut 机制决定了 QA 上下文的质量，移植时要保持同样的约束（尤其是图片触发 Cut、消息长度检查以及 system prompt 拼接）。</li><li><strong>沿用评分 prompt</strong>：若要比较新旧 parser 的表现，建议直接使用 <code>CLEAN_PROMPT</code> / <code>ONLINE_LLM_CLEAN_PROMPT</code>，确保评分口径一致，再考虑扩展额外的指标。</li><li><strong>保留输出结构</strong>：<code>messages</code> 数组 + <code>system</code> + <code>images</code> 的 schema 已与 LLaMA Factory 适配，建议在 Qing-Agent 的 <code>clean llm --parser default</code> 中继续输出相同字段，方便后续训练脚本读取 <code>dataset_info.json</code> 进行切换。</li></ol><h2 id="_11-qing-agent-集成方式" tabindex="-1">11. Qing-Agent 集成方式 <a class="header-anchor" href="#_11-qing-agent-集成方式" aria-label="Permalink to &quot;11. Qing-Agent 集成方式&quot;">​</a></h2><ul><li>通过 <code>python cli.py data clean llm --parser default</code> 调用 结构化 风格流程：先按 CSV 聚合+过滤，再复用 LLM 打分与 <code>accept_score</code> 阈值。<code>setting.jsonc</code> 的 <code>data_args.clean_set_args.llm_parser</code> 也可以设置为 <code>default</code> 以长期启用。</li><li>相关配置映射：<code>media_dir</code>、<code>max_image_num</code>、<code>add_time_to_system</code>、<code>vision_api.*</code>、<code>include_type</code>、<code>blocked_words</code> 等字段直接生效；当 <code>vision_api.enable=true</code> 且提供 API 信息时，会在清洗阶段自动完成图片描述并清空 <code>images</code> 列表。</li><li>输出仍保存到 <code>dataset/sft.jsonl</code>（或自定义 <code>data_path</code>），后续 <code>train_sft</code> 会读取 <code>_scored.csv</code> 与 <code>&lt;dataset&gt;-cleaned</code>，沿用原有训练链路。</li></ul><p>通过以上梳理，可以在不破坏现有清洗逻辑的前提下，为 Qing-Agent 新增默认 parser，并将 结构化 的数据清洗策略与 LLM 评分机制完整移植。</p>`,39)])])}const _=o(d,[["render",i]]);export{p as __pageData,_ as default};
